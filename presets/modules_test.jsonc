{
    "logo": null,
    "modules": [
        {
            "type": "memory",
            "format": "lua:local used, total = ...\nlocal percent = (used / total) * 100\nif percent > 80 then\n    return string.format('\\u001b[31mCritical Memory Usage: %.2f%%\\u001b[0m', percent)\nelse\n    return string.format('Memory Ok: %.2f%%', percent)\nend"
        }
    ]
}